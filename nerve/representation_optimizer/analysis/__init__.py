"""Architecture-neutral algebraic and structural analysis.

Import the concrete concern module (`engine`, `evidence`, `context`, or
`tensor_repository`) explicitly.  Keeping this boundary passive prevents
package validation from importing every analyzer implementation.
"""
