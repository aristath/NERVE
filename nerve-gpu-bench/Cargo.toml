[package]
name = "nerve-gpu-bench"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Small standalone target and link benchmarker for NERVE placement planning"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
